#include <iostream>
#include <cmath>
#include <iomanip>
using namespace std;
struct fun
{
	double *x;
	double *y;
};
fun trig(double a, double b, double c, double f);
void out(fun &r, double f);
int main()
{
	double a, b, c;
	cout<<"Enter limits and step:"<<endl;
	cin >> a >> c >> b;
	double f;
	f = (b - a) / c;
	fun r;
	r = trig(a, b, c, f);
	out(r, f);
	return 0;
}
fun tring(double a, double b, double c, double f)
{